query TournamentSync($slug: String!, $perPage: Int!) {
  tournament(slug: $slug) {
    id
    name
    slug
    events {
      id
      name
      sets(page: 1, perPage: $perPage, sortType: STANDARD) {
        nodes {
          id
          fullRoundText
          round
          phaseGroup {
            displayIdentifier
            phase {
              name
            }
          }
          state
          winnerId
          entrant1Source {
            typeId
            condition
            conditionString
          }
          entrant2Source {
            typeId
            condition
            conditionString
          }
          winnerProgressionSeed {
            id
            seedNum
          }
          loserProgressionSeed {
            id
            seedNum
          }
          slots {
            seed {
              id
              seedNum
            }
            entrant {
              id
              name
            }
            standing {
              stats {
                score {
                  value
                }
              }
            }
          }
        }
      }
    }
  }
}
